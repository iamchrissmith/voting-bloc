%h3 All Results:
- if @election.results.empty?
  %p No votes for this election
- else
  = react_component "Results", results: @election.results
  -# - @election.results.each do |result|
  -#   %p #{result[0]}: #{result[1]}
